[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "segmentlist"
requires-python = ">=3.10"
dynamic = ["version"]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.11.5",
]
